const Activity = {
  CHECK_IN: `check-in`,
  SIGHTSEEING: `sightseeing`,
  RESTAURANT: `restaurant`
};

const TRIP_TYPES = [
  `taxi`,
  `bus`,
  `train`,
  `ship`,
  `transport`,
  `drive`,
  `flight`
];

const ACTIV_TYPES = [
  `check-in`,
  `sightseeing`,
  `restaurant`
];

export {Activity, TRIP_TYPES, ACTIV_TYPES};
